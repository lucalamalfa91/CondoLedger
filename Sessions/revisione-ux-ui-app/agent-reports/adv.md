# Cluster ADV — Impostazioni Avanzate

## Schermata: Avanzate (backup + registro movimenti)
Sotto-tab `data-subview-panel="avanzate"` (`index.html:436-450`), raggiungibile da Impostazioni → sub-nav "Avanzate" (`index.html:392`). Contiene due card indipendenti: "Backup e ripristino" (export/import JSON) e "Registro movimenti" (tabella di sola consultazione, `id="movementsAdv"`). Questa vista nasce dalla risoluzione dichiarata di **UX-006** (`Sessions/verifica-finale-ux-prodotto/analysis-report.md:56`: *"Dati → Registro `#movements` — Duplica dovuti+versamenti già in Movimenti… 'Dove sono i dati veri?' → Spostare in Impostazioni → Avanzate o rimuovere"*). Il codice conferma che lo spostamento è avvenuto, ma l'analisi mostra che il problema concettuale (dove sono i dati "veri" e modificabili) non è stato realmente risolto, solo spostato, e in un caso è addirittura peggiorato su mobile.

| ID | Severità | Dove (file:riga) | Problema | Impatto utente | Fix proposto | Sforzo | Stato vs. report precedenti |
|---|---|---|---|---|---|---|---|
| ADV-001 | Critical | `css/app.css:366,401` (`@media max-width:860px{ .panel-table-wrap{display:none} }`); `index.html:448` (`class="data-table-wrap panel-table-wrap" id="movementsAdv"`); `js/render.js:969-981` (`renderMovements`) | Sotto 860px il contenitore del Registro movimenti viene forzato a `display:none`. A differenza di Dovuti/Versamenti/Saldi (`js/render.js:553,599,622` che usano `dataListHtml()` da `js/mobile-cards.js:9-12` per produrre un fallback a card), `renderMovements` genera **solo** un `<table>`, senza alcuna versione a card. Risultato: su smartphone la card "Registro movimenti" appare vuota (niente tabella, niente card, nemmeno il messaggio "Nessun movimento…", perché tutto il wrapper è nascosto). | L'utente su mobile che cerca l'audit trail (es. commercialista/proprietario che consulta da telefono) non vede assolutamente nulla, senza alcun indizio che il contenuto esista ma sia nascosto. | Applicare lo stesso pattern `dataListHtml(tableHtml, cardsHtml)` già usato da `renderDues`/`renderPayments`, generando una card-list per il registro; oppure rimuovere `panel-table-wrap` da questo contenitore specifico se si preferisce mantenere solo lo scroll orizzontale della tabella. | M | NUOVO — la sessione `Sessions/fix-layout-mobile-responsive/verification.md:53` (AC-US007-02) dichiarava "nessuna duplicazione visibile" per tutti gli elementi `.panel-table-wrap`, ma non ha verificato che `movementsAdv` non avesse un fallback card equivalente a `duesTable`/`paymentsTable`: la verifica precedente ha un gap non intercettato. |
| ADV-002 | High | `index.html:447` (sottotitolo *"Vista auditoria: tutti i dovuti e versamenti"*); `js/render.js:59-62` (`rowActions`) e `:979` (chiamata in `renderMovements`); `js/main.js:372-400` (`handleRecordAction`); `js/main.js:271-303` (`startEditDue`/`startEditPayment` → `navigate('movimenti', …)`) | Il registro è etichettato esplicitamente come "vista auditoria" (sola consultazione), ma ogni riga espone bottoni "Modifica"/"Elimina" pienamente funzionanti: cliccando "Modifica" l'utente viene **silenziosamente reindirizzato** dalla vista Impostazioni alla vista Movimenti (tab e sotto-tab diversi) con il form di modifica pre-popolato, senza alcun messaggio di transizione. | Contraddice l'etichetta stessa della sezione: non è affatto "solo audit", è un secondo punto di modifica dati con navigazione a sorpresa fuori da Impostazioni. Il problema originale di UX-006 ("dove sono i dati veri?") non è risolto: si è solo spostato E aggravato, perché ora una vista dichiarata "read-only" permette CRUD completo. | O rendere il registro realmente sola-lettura (rimuovere `rowActions` da `renderMovements`) rimandando alla nota "modifica in Movimenti", oppure rinominare la sezione per riflettere che è anche operativa, evitando il claim "vista auditoria". | S | PARZIALMENTE RISOLTO (rif. UX-006) — lo spostamento in Impostazioni→Avanzate è avvenuto come suggerito, ma l'implementazione ha mantenuto capacità di modifica/eliminazione, quindi il problema concettuale segnalato ("dove sono i dati veri") persiste in una forma nuova. |
| ADV-003 | Medium | `index.html:438` (*"Export/import JSON v4 — uso tecnico o migrazione"*); `js/main.js:720` (`a.download = 'spese-condominiali-v3.json'`); `js/config.js:5` (`export const JSON_SCHEMA_VERSION = 5`) | Tre riferimenti di versione diversi e incoerenti per lo stesso file di backup: la UI dice "v4", il nome del file scaricato dice "v3", lo schema reale prodotto/atteso è v5 (`parseBackup` in `js/backup.js:53-61` gestisce v2→v5). | Un utente tecnico che gestisce più backup nel tempo (uso dichiarato: "uso tecnico o migrazione") non può fidarsi dell'etichetta per capire compatibilità/età del file. | Allineare copy e nome file alla costante `JSON_SCHEMA_VERSION` (es. template string), evitando hardcoding sparso. | S | NUOVO |
| ADV-004 | Medium | `js/main.js:725-744` (`importJson`); `js/api.js:688-706` (`syncBackupToSupabase`) | L'import è "alla cieca": il dialog di conferma (`confirmDialog('Importare il backup su Supabase? Le case verranno aggiunte al tuo account.')`) non mostra alcuna anteprima del contenuto (quante case, quali nomi, quale intervallo di date). `syncBackupToSupabase` inserisce sempre **nuove** case con nuovo id (`id: uid('house')`), senza controllo su nomi già esistenti: reimportare lo stesso file due volte (es. doppio click, o ripetuto dopo un errore percepito) crea case duplicate silenziosamente, e non c'è alcun riepilogo post-import ("N case, M dovuti importati"). | Non c'è rischio di sovrascrittura distruttiva (buona notizia: è additivo), ma c'è rischio concreto di duplicazione silenziosa di intere case senza modo di accorgersene se non scorrendo manualmente l'elenco case. | Aggiungere anteprima nel dialog di conferma (nomi case + conteggio dovuti/versamenti dal file parsato) e un controllo soft-duplicate su nome casa con avviso esplicito; mostrare un toast di riepilogo con i conteggi effettivamente importati. | M | NUOVO |
| ADV-005 | Medium | `index.html:440` (`<label class="btn btn-secondary" for="importFileAdv">Importa JSON</label>`); `css/app.css:187` (`.file-input { display:none; }`) | Il controllo "Importa JSON" è un `<label>` associato a un `<input type="file">` nascosto (`display:none`), senza `tabindex`/`role="button"`. Gli elementi `<label>` non sono nativamente nel tab-order della tastiera: un utente che naviga con Tab **salta** completamente il controllo di import. | Funzione di import backup non raggiungibile via tastiera, in contrasto con il resto del design system che dichiara focus visibile "per tutti i controlli interattivi" (`design_bundle/.../README.md:170-171`). | Aggiungere `tabindex="0"` + gestione `keydown` (Enter/Spazio → click sull'input), o sostituire con un `<button>` che triggera `input.click()` programmaticamente. | S | NUOVO |
| ADV-006 | Low | `js/toast.js:10,22-26,41-43` (`showToast`/`toastError` condividono `role="status"` + `aria-live="polite"`) | Un import fallito (JSON non valido, versione schema non supportata — messaggi da `js/backup.js:54,60`) viene comunicato solo con un toast che scompare dopo 6s, con `aria-live="polite"` anche per gli errori (dovrebbe essere `assertive`/`role="alert"` per essere annunciato con priorità), e senza traccia persistente nella card "Backup e ripristino". | Se l'utente non nota il toast (o usa uno screen reader con priorità polite), non ha modo di sapere che l'import è fallito né perché, e non può rivedere il messaggio dopo la scomparsa. | Per gli errori di import, aggiungere anche un'area di errore inline nella card Backup (pattern già usato altrove nell'app, es. `accountPasswordError` in `index.html:456`) oltre al toast, e usare `role="alert"`/`aria-live="assertive"` per `toastError`. | S | NUOVO |
| ADV-007 | Medium | `index.html:97` (nav-rail: *"Impostazioni — Casa e account"*, nessuna menzione di Avanzate/backup); `index.html:389-392` (sub-nav "Immobili · Account · Avanzate"); `css/app.css:397` (`.sub-nav{overflow-x:auto}` su mobile) | Il registro/backup è sepolto a 3 livelli (Impostazioni → scroll orizzontale del sub-nav su mobile → tab "Avanzate"), e l'etichetta di primo livello ("Casa e account") non lascia intuire che lì dentro esista anche backup/audit. Combinato con ADV-001, su mobile il percorso porta a una schermata di fatto vuota per il registro. | Bassa scopribilità: un utente che cerca "dove vedo tutti i movimenti" o "dove faccio un backup" non troverebbe indizi nel testo di navigazione principale. | Aggiornare il sottotitolo nav-rail per menzionare "Backup/audit", o aggiungere un badge/hint nella card Impostazioni → Immobili che rimanda ad Avanzate. | S | PARZIALMENTE RISOLTO (rif. UX-006) — spostare la vista ha ridotto la duplicazione visibile in "Movimenti", ma ha introdotto un problema di wayfinding non presente/valutato nel report originale. |
| ADV-008 | Low | `js/render.js:969-980` (`renderMovements`) | Il registro elenca **tutti** i dovuti+versamenti di una casa, su tutti gli esercizi fiscali, in un'unica tabella ordinata solo per data, senza filtro per esercizio, ricerca o paginazione (a differenza di `renderPayments`, che applica `getFilteredPayments`, `js/render.js:577-578`). Per case con più anni di storico, la tabella "audit" diventa lunga e poco scansionabile proprio nello use-case (verifica puntuale) per cui è stata pensata. | Difficoltà a trovare un singolo movimento storico in una casa con anni di dati; l'unico aiuto è lo scroll interno della tabella (`min-width:36rem`, `css/app.css:110`). | Aggiungere un filtro per esercizio fiscale (riutilizzando il pattern già presente per Versamenti) o un raggruppamento visivo per anno. | M | NUOVO |
