# Cluster DASH — Panoramica

## Schermata: Panoramica
La schermata (`index.html:138-163`) è composta da tre blocchi verticali: (1) hero di conformità `#complianceHero` (`js/compliance-status.js` + `renderComplianceHero()` in `js/render.js:393-421`) con headline, fatti chiave e 2 CTA; (2) un blocco titolo casa + toolbar rapida (nascosta su mobile); (3) il pannello "La tua situazione" con filtro esercizio, griglia KPI (`js/kpi-metrics.js` + `renderPanoramicaKpis()` in `render.js:451-513`) e mini-lista "Altri esercizi". La struttura corrisponde a quanto pianificato in `Sessions/rendiconti-kpi-situazione-panoramica/plan.md` (US-003/US-004) e ha eliminato le vecchie tabelle `#annualTableWrap`/`#dashboardPayments` segnalate in `analysis-report.md` (UX-001, MOB-011). Il layout gerarchico di base è corretto (hero in cima, card "saldo" primaria a tutta larghezza), ma persistono ridondanze di contenuto, incoerenze di terminologia/wayfinding, una lacuna di stile per il tono "attenzione/saldato" e l'assenza di uno stato di errore distinto dallo stato vuoto.

| ID | Severità | Dove (file:riga) | Problema | Impatto utente | Fix proposto | Sforzo | Stato vs. report precedenti |
|---|---|---|---|---|---|---|---|
| DASH-001 | High | `js/compliance-status.js:68-78` (facts: Esercizio, saldo, prossima rata) + `js/render.js:474-487` (kpi-card saldo/prossima-rata) | Hero "Sei in regola" e pannello KPI mostrano due volte gli stessi dati (esercizio, saldo, prossima rata) in due componenti visivamente distinti e adiacenti | Diluisce l'obiettivo dichiarato in US-003 ("capire in pochi secondi se devo agire"); l'utente deve confrontare due box per capire se sono davvero la stessa cifra | Rimuovere dai `facts` dell'hero i valori già coperti dalla card KPI primaria (tenere solo l'headline/CTA nell'hero), o fondere i due blocchi in un unico componente | M | PARZIALMENTE RISOLTO (rif. plan.md rischio "Ridondanza hero vs KPI grid", riga 138 — mitigazione prevista era solo rimuovere `#metrics` legacy, non la sovrapposizione hero/KPI) |
| DASH-002 | Medium | `js/kpi-metrics.js:154` (Eccedenza/In debito/Pareggio) vs `js/situazione-report.js:122-135` e `js/compliance-status.js:68-78` (Saldo positivo/Saldo negativo/Saldo) | Stesso concetto (segno del saldo) descritto con due lessici diversi nella stessa schermata; il design system (`design_bundle/.../README.md`, sezione Voice) fissa "Sei in eccedenza/Sei in debito" come voce prodotto, da cui la card principale si discosta | Confonde l'utente su cosa significhi "Saldo negativo" vs "In debito" per esercizi diversi nella stessa vista | Allineare le etichette (es. usare sempre "Eccedenza/In debito/Pareggio" anche nella card e nell'hero, o "Saldo positivo/negativo" ovunque) | S | NUOVO |
| DASH-003 | Medium | `index.html:159` (`Rendiconto completo` → subview `situazione`) vs `js/compliance-status.js:100,113,128,155` (`Vedi situazione`) e tab reale `index.html:172` (`Situazione`) | Il pulsante di chiusura pannello si chiama "Rendiconto completo" ma porta alla tab "Situazione"; altri 3 CTA per la stessa destinazione la chiamano "Vedi situazione" | Wayfinding incoerente: l'utente clicca "Rendiconto completo" aspettandosi una sezione "Rendiconto" e atterra su "Situazione", nome mai citato nel link | Rinominare il pulsante in "Vedi situazione" (o "Situazione completa") per coerenza con le altre CTA e col nome reale della tab | S | NUOVO |
| DASH-004 | Medium | `css/app.css:149` (`.positive`/`.negative`/`.warning`, non `.warn`), `:181` (`.badge.warn`/`.badge.success` scoped) vs `js/render.js:402` (`compliance-fact-value ${f.tone}`) e `:483` (`kpi-card-value ${card.tone}`); tone `'warn'`/`'success'` assegnati in `js/situazione-report.js:126,134` e `js/kpi-metrics.js:81` | Le classi di testo `warn` e `success` non hanno una regola CSS generica (esistono solo per `.badge`), quindi il valore testuale di "Prossima rata" (hero) e delle card con esito "pareggio"/"saldato in esercizio successivo" resta col colore di default invece di arancio/verde | Nei casi "attenzione" (rata in scadenza, pareggio) e "saldato", il segnale visivo di colore manca proprio dove servirebbe rinforzare la lettura rapida dello stato | Aggiungere `.compliance-fact-value.warn`, `.kpi-card-value.warn`, `.success` generiche riusando i token `--color-warning`/`--color-success` già definiti | S | NUOVO |
| DASH-005 | Medium | `index.html:152-155` (`.panel-head` con h2+p a sinistra, `<select id="periodFilter">` a destra); `css/app.css:177` (`.panel-head { display:flex; ...}` senza `flex-wrap` né override in nessuna media query) | A differenza di `.hero` (che nasconde la toolbar sotto 860px), `.panel-head` non ha una regola mobile dedicata: su viewport stretti (390px) titolo + sottotitolo lungo ("Indicatori sintetici — apri Situazione...") e select possono schiacciarsi o generare overflow | Rischio di violare il criterio già fissato in passato "nessuno scroll orizzontale in Panoramica" (R1/MOB-001) per un elemento introdotto dopo quella verifica | Aggiungere `flex-wrap: wrap` a `.panel-head` o impilare titolo/select sotto una soglia mobile, verificando su 390×844 | S | NUOVO (da verificare su device reale; contigua allo spirito di R1 in `analysis-report-after-devils-review.md:24`) |
| DASH-006 | High | `js/auth.js:7-13` (`loadHouseData` cattura l'errore solo con `toastError`) e `:126-146` (`restoreSession` chiama `loadHouseData` senza propagare stato di errore); `js/render.js:1419-1451` (`renderEmptyState`) | Se il fetch iniziale delle case/dovuti fallisce (rete, RLS, timeout Supabase), la Panoramica mostra lo stesso empty state di "Nessun immobile registrato" con pulsante "Aggiungi immobile"; l'unico segnale di errore è un toast transitorio che può sparire prima che l'utente lo legga | Un utente con dati reali ma un fallimento temporaneo di rete può credere di aver perso l'immobile/i dati e non capire che deve solo ricaricare | Distinguere esplicitamente uno stato "Errore di caricamento" (banner persistente + azione "Riprova") dallo stato "Nessun dato" nella Panoramica | M | NUOVO |
| DASH-007 | Medium | `js/render.js:461-468` (`panoramicaScopeNote`, testo lungo in classe `.subtle`/`.muted`, `css/app.css:129`, `font-size: var(--text-sm)`, colore attenuato) | Quando l'esercizio selezionato nel filtro differisce da quello mostrato nell'hero, l'avviso ("Il riquadro «Sei in regola» in alto si riferisce a…") è comunicato con testo piccolo, a bassa enfasi visiva, in una frase lunga | L'utente può cambiare filtro, leggere i KPI di un altro esercizio e continuare a pensare che si riferiscano allo stesso "Sei in regola" mostrato sopra, fraintendendo la propria posizione debitoria/creditoria | Evidenziare il disallineamento con un badge o colore di attenzione, non solo testo muted; accorciare la frase | S/M | NUOVO |
| DASH-008 | Low | `js/render.js:1426-1432,1448-1450` (riferimenti a `els.metrics`, `els.annualTableWrap`, `els.annualCards`, `els.dashboardPayments`) confrontati con `index.html` (questi id non esistono più nel markup) | Codice morto rimasto dopo la rimozione delle vecchie tabelle di Panoramica (UX-001); nessun impatto utente diretto (i controlli `if (els.x)` sono no-op) ma rischio di confusione/manutenzione futura e di regressioni silenziose se qualcuno riaggiunge quei nodi con altro scopo | Rimuovere le righe morte e i relativi handler duplicati (`emptyAddHouseBtn` è cablato due volte, righe 1445 e 1448) | S | PARZIALMENTE RISOLTO (rif. `analysis-report.md` UX-001 — markup ripulito, JS no) |
| DASH-009 | Low | `css/app.css:287-292` (`.kpi-card--primary` con `color-mix(in srgb, var(--color-surface-offset) 65%, var(--color-primary) 35%)` come sfondo, testo sopra non ricolorato) in tema scuro (`--color-primary: #4f98a3` a riga 19) | Non verificato con strumento di contrasto: il tint teal al 35% sotto testo di default in dark mode potrebbe avvicinarsi alla soglia WCAG AA per testo normale/bold | Possibile leggibilità ridotta della card più importante ("Saldo") proprio nel tema scuro | Verificare contrasto con un tool (es. axe/Lighthouse) su entrambi i temi e regolare l'opacità del mix se sotto 4.5:1 | S | NUOVO |
