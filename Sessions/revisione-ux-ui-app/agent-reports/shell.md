# Cluster SHELL — Navigazione, shell, overlay, design system trasversale

## Area: Navigazione (nav rail / bottom nav / header)

Le etichette di primo livello sono coerenti tra desktop (`nav-rail`, `index.html:94-97`) e mobile (`bottom-nav`, `index.html:478-482`): Panoramica / Movimenti / Impostazioni sono identiche nei due contesti. Il punto debole è sotto la superficie: la quarta voce del bottom-nav ("Situazione") è uno shortcut verso una sub-tab di Movimenti che non ha equivalente nel nav-rail desktop, e l'header comunica il contesto-casa solo tramite una `<select>` che tende a essere il segnale più debole della UI.

| ID | Severità | Dove (file:riga) | Problema | Impatto utente | Fix proposto | Sforzo | Stato vs. report precedenti |
|---|---|---|---|---|---|---|---|
| SHELL-001 | Medium | `index.html:481` (bottom-nav 4ª voce `data-nav-mobile-subview="situazione"`) vs `index.html:94-97` (nav-rail 3 voci) e `index.html:172` (sub-nav-btn "Situazione" dentro Movimenti) | Il bottom-nav promuove "Situazione" a voce di primo livello (4 tab) mentre il nav-rail desktop la lascia come sub-tab nascosta dentro Movimenti (3 voci) | IA asimmetrica tra device: l'utente desktop non ha lo stesso "fast path" verso Situazione che ha su mobile | Aggiungere uno shortcut equivalente nel nav-rail (es. voce secondaria o badge) oppure documentare esplicitamente l'asimmetria come scelta intenzionale | S | PARZIALMENTE RISOLTO (rif. MOB-005) — MOB-005 chiedeva di risolvere la scomparsa di "Situazione" nello scroll orizzontale mobile: risolto solo lato mobile via shortcut bottom-nav, non normalizzato su desktop |
| SHELL-002 | Medium | `js/render.js:111-115` (`updateHeader`, testo da `viewHeading` statico) + `index.html:108` (`houseSelect`) | Il sottotitolo header (`#viewSubtitle`) è un testo statico per view, non include mai il nome della casa attiva; l'unico segnale di "in che casa sono" è la select, che su mobile è troncata (`ellipsis`) | Rischio di disorientamento su multi-immobile, specie dopo uno switch rapido da drawer | Includere il nome casa nel sottotitolo o in un badge accanto al titolo pagina; aggiungere un annuncio minimale (es. `aria-live="polite"`) al cambio casa | S/M | PARZIALMENTE RISOLTO (rif. UX-002) — UX-002 lamentava "tre indicatori di contesto"; ridotti, ma il segnale casa resta debole |
| SHELL-003 | Low | `css/app.css` media query `≤860px` — `.header-titles .muted{display:none}` + `.house-bar` (2 icon-btn 44px + select flex) + `.user-chip` tutti sulla stessa riga header | Su viewport molto stretti (360-375px) l'header cumula: icona drawer (44px) + select + icona "+casa" (44px) + titolo pagina + chip utente, tutto su una riga; non risulta testato a runtime sotto 390px | Rischio wrapping/clip non verificato | Misurare a 360px; eventualmente nascondere l'icona "Gestisci immobili" quando la select stessa permette lo switch | S | NUOVO (gap di verifica: `fix-layout-mobile-responsive/verification.md` segnala esplicitamente AC-US001-04 "360px non misurato a runtime") |
| SHELL-004 | High | `css/app.css:54` — regola `:focus-visible` limitata a `.nav-btn, .sub-nav-btn, .bottom-nav-btn, .house-select, .add-house-btn, .fab` | Nessun anello di focus visibile su `.icon-btn` generico, `.house-drawer-btn` (bottone "Gestisci immobili", `index.html:105`), `.sheet-close`/`.form-sheet-close` (×4), `.user-chip` (`userMenuBtn`) | Utenti da tastiera non vedono dove si trova il focus su azioni chiave trasversali (apri drawer, chiudi sheet, menu utente) | Estendere il selettore `:focus-visible` a `.icon-btn, .user-chip, .sheet-close, .form-sheet-close` | S | PARZIALMENTE RISOLTO (rif. US-010, `redesign-layout-ux-mobile-desktop`) — US-010 segnato PARTIAL ("keyboard tab avanzato deferred"); il gap sul focus ring degli icon-btn non era stato esplicitamente isolato. Nota: il design system dichiara (`design_bundle/.../README.md`, sez. Focus) "outline... for all interactive controls" come intento esplicito, non rispettato ovunque |

## Area: House drawer e quick-add FAB

Positivo da segnalare: lo sheet quick-add (`index.html:490-497`) apre ora "Importa documento" in ≤2 tap dalla Home (FAB → voce → `data-close-sheet`), e navigando a "Nuovo versamento"/"Nuovo dovuto" apre automaticamente il relativo form-sheet (`js/main.js:786-791`). Questo di fatto soddisfa R2 (import raggiungibile in ≤2 tap) e riduce il carico cognitivo per registrare un versamento a 2 tap prima della compilazione. Il drawer immobili, invece, resta un pattern di overlay "artigianale" diverso da quello usato per i dialog di conferma.

| ID | Severità | Dove (file:riga) | Problema | Impatto utente | Fix proposto | Sforzo | Stato vs. report precedenti |
|---|---|---|---|---|---|---|---|
| SHELL-005 | High | `index.html:502` (`house-drawer`), `index.html:490` (`quickAddSheet`); `js/main.js:190-201` (`openHouseDrawer/closeHouseDrawer`), `js/main.js:746-754` (`openQuickAddSheet/closeQuickAddSheet`) | Overlay gestiti con solo `classList.toggle`, senza focus trap, senza spostare il focus nell'overlay all'apertura, senza restituirlo al trigger alla chiusura | Utenti tastiera/screen reader possono tabbare fuori dall'overlay nel contenuto sottostante; dopo la chiusura il focus resta "perso" | Impostare `.focus()` sul primo elemento utile all'apertura, ripristinare il focus sul trigger alla chiusura, aggiungere un trap minimo su Tab | M | NUOVO |
| SHELL-006 | Medium | `index.html:490` (`role="dialog" aria-labelledby`, ma senza `aria-modal`) vs `index.html:502` (`<aside aria-label aria-hidden>`, senza `role="dialog"`) | Due pattern ARIA diversi per lo stesso concetto di overlay bloccante (sheet vs drawer) | Comportamento incoerente per tecnologie assistive tra due overlay dall'aspetto/funzione simile | Uniformare a `role="dialog" aria-modal="true"` su entrambi, o migrare a `<dialog>` nativo come già fatto per confirm/onboarding | M | NUOVO |
| SHELL-007 | Low | `js/main.js:811-813` (`houseSelect` change) e `js/main.js:826-830` (`houseDrawerList` click) | Su mobile coesistono due controlli che cambiano la casa attiva (select nativa + drawer), senza distinzione di scopo comunicata (drawer = gestione completa, select = switch rapido) | Ridondanza percepita, non è chiaro quale usare | Etichettare/differenziare visivamente, o unificare (es. tap sulla select apre il drawer) | S | PARZIALMENTE RISOLTO (rif. MOB-004, FLOW-007) — il drawer è stato implementato e cablato (in precedenza "CSS definito ma non usato in HTML"); resta la doppia via di selezione |

## Area: Dialog di conferma e toast

Verifica positiva: non risultano più `alert()`/`confirm()` nativi in `js/main.js` (grep negativo) — tutte le conferme passano da `confirmDialog()` (`js/confirm.js`), che usa `<dialog>.showModal()` nativo (focus trap automatico). Restano però gap nella qualità del contenuto dei messaggi e un bug concreto di stacking sui toast.

| ID | Severità | Dove (file:riga) | Problema | Impatto utente | Fix proposto | Sforzo | Stato vs. report precedenti |
|---|---|---|---|---|---|---|---|
| SHELL-008 | Medium | `index.html:513` (`<dialog id="confirmDialog">`, nessun `aria-labelledby`/`aria-describedby`) | Titolo (`#confirmDialogTitle`) e corpo (`#confirmDialogBody`) non sono associati esplicitamente al `<dialog>` via ARIA | Screen reader potrebbero non annunciare titolo/messaggio in modo affidabile all'apertura | Aggiungere `aria-labelledby="confirmDialogTitle" aria-describedby="confirmDialogBody"` sul `<dialog>` | S | NUOVO |
| SHELL-009 | High | `js/main.js:1044` — `` `Eliminare ${house.name}?` `` | Il messaggio di conferma per l'eliminazione casa è generico: non avvisa che verranno cancellati a cascata tutti dovuti, versamenti, esercizi fiscali, saldi precedenti e import legati a quella casa, né propone un export di backup prima | È l'azione più distruttiva/irreversibile dell'app ma ha lo stesso livello di conferma di un singolo dovuto | Messaggio esplicito con conteggio dati coinvolti + CTA "Esporta backup prima di eliminare" per case non vuote | S/M | PARZIALMENTE RISOLTO (rif. UX-008) — UX-008 chiedeva di sostituire `alert`/`confirm` nativi: fatto ovunque; la qualità del messaggio per l'azione a più alto rischio resta debole |
| SHELL-010 | Medium | `js/main.js:592,626` (riepiloghi import con `\n`) + `js/confirm.js:16` (`bodyEl.textContent`) + `css/app.css:129` (`.subtle` senza `white-space:pre-line`) | I messaggi multi-riga (riepilogo/avvisi import) vengono iniettati come testo semplice ma il CSS non preserva gli a-capo: appaiono come un unico blocco compresso | Riepilogo pre-conferma di un import difficile da leggere prima di un'azione delicata | Aggiungere `white-space: pre-line` a `#confirmDialogBody`, o costruire liste HTML invece di stringhe con `\n` | S | NUOVO |
| SHELL-011 | High | `css/app.css` — `--z-fab:20`, toast-host `z-index: calc(var(--z-fab) + 2)` = 22, vs `--z-drawer:30` (house-drawer-backdrop) e `--z-sheet:40` (quickAddBackdrop) | Un toast mostrato mentre il drawer immobili o lo sheet quick-add sono aperti risulterebbe visivamente coperto dal backdrop semitrasparente (z 30/40 > 22), quindi invisibile/non cliccabile nonostante `pointer-events:auto` | Feedback (es. errore su selezione casa) potenzialmente invisibile all'utente in quel contesto | Portare il toast-host sopra tutti gli overlay, es. `z-index: calc(var(--z-sheet) + 10)` | S | NUOVO |
| SHELL-012 | Low | `js/toast.js:24-38` — `showToast` crea un `<div>` senza `tabindex`/ruolo interattivo | Il toast è dismissibile solo via click mouse/touch prima del timeout (4.2s successo / 6s errore); un utente da tastiera non può chiuderlo anticipatamente | Basso impatto (il timeout comunque lo rimuove), ma inconsistente con `role="status"` già presente | Rendere il toast un `<button>` o aggiungere `tabindex="0"` + gestione Enter/Space | S | NUOVO |

## Area: Design token e coerenza visiva trasversale

| ID | Severità | Dove (file:riga) | Problema | Impatto utente | Fix proposto | Sforzo | Stato vs. report precedenti |
|---|---|---|---|---|---|---|---|
| SHELL-013 | High | `css/app.css:1-21` — `--color-text-muted: #7a7974` (tema chiaro) su `--color-bg:#f7f6f2` | Contrasto calcolato ≈ 4.03:1, sotto la soglia WCAG AA 4.5:1 per testo normale (passa solo come large-text/UI a 3:1). Nel tema scuro lo stesso token (`#979691` su `#171614`) raggiunge ≈6.1:1, ampiamente conforme | Il token è usato ovunque come testo secondario di sistema (`.muted/.subtle/.hint`, sotto-etichetta nav, `kpi-card-label`, `metric-label`, header `viewSubtitle`): l'incoerenza di contrasto tra i due temi è sistemica | Scurire leggermente `--color-text-muted` in tema chiaro (es. verso `#6b6a65`) per raggiungere 4.5:1 | S | NUOVO |
| SHELL-014 | Medium-High | `css/app.css` — 25 occorrenze di `rgba(0,0,0,.XX)` hardcoded (9× `.08`, 4× `.1`, 3× `.35`, 3× `.16`, 2× `.45`, 2× `.12`, 1× `.15`, 1× `.06`), es. `.card,.panel` (r.145), `.icon-btn` (r.139), `input,select,textarea` (r.31), `.user-chip` (r.203), `.login-card` (r.193) | Questi bordi/backdrop non usano il token theme-aware `--color-divider` (che correttamente varia da `#dcd9d5` a `#262523` tra i temi); in dark mode un bordo nero semi-trasparente su superfici già scure (`#1c1b19`/`#171614`) risulta quasi invisibile — l'effetto opposto di quanto serve per delimitare card/input/bottoni | Il dark theme perde leggibilità strutturale (bordi di card, input, icon-btn, chip appena percettibili) | Sostituire i valori hardcoded con `var(--color-divider)` o un nuovo token `--color-border` derivato per tema | M | NUOVO |

## Area: Accessibilità trasversale

Riepilogo puntuale su target tattili, ARIA e pattern tastiera, in aggiunta ai finding di focus/contrasto già dettagliati sopra (SHELL-004, SHELL-013).

| ID | Severità | Dove (file:riga) | Problema | Impatto utente | Fix proposto | Sforzo | Stato vs. report precedenti |
|---|---|---|---|---|---|---|---|
| SHELL-015 | Medium | `index.html:128-131` (`role="menu"`/`role="menuitem"` sul menu utente) + `js/main.js:994-1011` (solo `click`, nessuna gestione `ArrowUp/ArrowDown/Home/End/Escape` dedicata al roving focus) | La semantica ARIA dichiara un vero menu (che implica navigazione da tastiera con frecce) ma l'implementazione gestisce solo il click; non c'è roving tabindex | Utenti tastiera/screen reader che si aspettano il pattern "menu" (frecce) trovano solo Tab sequenziale, esperienza incoerente con il ruolo dichiarato | Implementare roving focus con frecce, o declassare a `role="group"`/lista di bottoni se non si vuole implementare il pattern completo | M | NUOVO |
| SHELL-016 | Info (risolto) | `css/app.css:137` `.row-actions .btn{min-height:44px;min-width:44px}`; `css/app.css:139` `.icon-btn{width:44px;height:44px}`; `.fab{width:56px;height:56px}` | — | Touch target di nav/FAB/azioni riga rispettano sistematicamente ≥44×44px | — | — | RISOLTO (rif. MOB-008) — confermato staticamente su tutte le classi coinvolte; nessuna regressione rilevata |
| SHELL-017 | Basso (verifica) | Skip link `index.html:29` → `#mainContent`; `document.addEventListener('keydown', Escape → closeAllOverlays)` `js/main.js:764-767` | — | Skip link e chiusura overlay via Escape funzionano in modo consistente su tutti gli overlay trasversali | — | — | RISOLTO — baseline positiva, nessuna azione richiesta |
