```markdown
# Cluster SIT — Situazione

## Schermata: Situazione
Vista di rendiconto per esercizio fiscale (`index.html` L369-385, `js/render.js` `renderSituazione()` L857-900): selettore esercizio, chip di riepilogo (`renderSituazioneSummaryChips`, L652-683, con aria-live), sezioni a fisarmonica (`situazioneCollapsibleSection`, L379-391) per Saldi anno precedente, Dettaglio rate preventivo, Consuntivo, Versamenti, Riporti, Versamenti senza rata, ed export PDF (`js/pdf-situazione.js`). Il saldo primario è ben gerarchizzato (chip "primary" più grande, `css/app.css` L216-230), ma la vista soffre di un problema di parità mobile/desktop non risolto sulle tabelle di dettaglio, di stato non persistito degli accordion, e di alcuni papercut minori su feedback e terminologia.

| ID | Severità | Dove (file:riga) | Problema | Impatto utente | Fix proposto | Sforzo | Stato vs. report precedenti |
|---|---|---|---|---|---|---|---|
| SIT-001 | Critical | `js/render.js` L685-711 (`renderPriorBalanceSection`), L713-733 (`renderRateTable`), L735-767 (`renderConsuntivoSection`), L769-807 (`renderPeriodPaymentsSection`/`renderConsuntivoPaymentsSection`), L809-841 (`renderCarrySection`/`renderUnlinkedSection`); `css/app.css` L90-110 | Tutte le 7 sezioni di dettaglio di Situazione emettono `<div class="data-table-wrap"><table>...` grezzo, senza mai passare per `dataListHtml()` (`js/mobile-cards.js`). Il CSS base (non dentro un `@media`) impone `table { min-width: 36rem }` (L110) sempre attivo: su viewport <576px (es. 375-390px) ogni tabella di rate/consuntivo/versamenti/riporti forza scroll orizzontale dentro l'accordion. Nello stesso file, `renderPriorBalances` (L602-623), `renderDashboardPayments` (L625-650), `duesTable`/`paymentsTable` (L553, 599) già usano il pattern `dataListHtml(tableHtml, cardsHtml)` per convertirsi in `.data-card` sotto 860px (`mobile-cards.js` `isMobileLayout()`), ma questo non è mai stato applicato alle sezioni di Situazione. | Su smartphone, controllare il dettaglio rate/versamenti/riporti in Situazione richiede scroll orizzontale dentro un riquadro già scrollabile verticalmente (accordion + tabella con doppio scroll), difficile da usare con una mano, testo tagliato ai lati. | Estendere il pattern già esistente: per ciascuna delle 6-7 funzioni generare anche `cardsHtml` (una `.data-card` per riga, come già fatto per `priorBalancesTable`) e sostituire `<div class="data-table-wrap">...</div>` con `dataListHtml(tableHtml, cardsHtml)` prima di passare `bodyHtml` a `situazioneCollapsibleSection`. | M | **PARZIALMENTE RISOLTO (rif. MOB-001)** — MOB-001 (analysis-report.md L19,L70) descriveva il problema come generale a "tutte `.data-table-wrap`" / "ogni schermata operativa"; il fix (`mobile-cards.js` + `dataListHtml`) è stato costruito e applicato a Dovuti/Versamenti/Panoramica/Saldi precedenti, ma i criteri di rilascio R1 (analysis-report-after-devils-review.md L24) hanno ristretto lo scope esplicito a "Dovuti, Versamenti, Panoramica pagamenti", **omettendo Situazione** che presenta ancora esattamente il difetto originale. Non è una regressione (il codice di Situazione non è mai cambiato), ma una copertura incompleta del fix. |
| SIT-002 | Medium | `js/render.js` L379-391 (`situazioneCollapsibleSection`, nessun attributo `open`), L891-899 (`renderSituazione` ricostruisce `els.situazioneSections.innerHTML` da zero ad ogni chiamata) | Tutti gli accordion (Saldi precedente, Rate, Consuntivo, Versamenti, Riporti, Versamenti senza rata) partono sempre chiusi, anche al primo caricamento della vista, quindi l'utente deve aprire manualmente più sezioni per vedere qualunque dato puntuale (es. se una rata è stata pagata). Inoltre `render()` è invocato globalmente dopo quasi ogni azione dell'app (`main.js` righe 194,416,424,547,660,910,1115,1164,1341) e rigenera sempre `innerHTML` senza salvare quali `<details>` erano aperti: se l'utente espande "Dettaglio rate" e poi compie un'azione altrove che richiama `render()`, la sezione si richiude silenziosamente perdendo il contesto. | Carico cognitivo aumentato (tutto nascosto per default in una vista già densa di numeri) e perdita di stato imprevedibile durante la sessione. | Mantenere in stato locale l'insieme degli id `.situazione-collapse[open]` prima di sovrascrivere `innerHTML` e riapplicare l'attributo `open` ai medesimi id dopo il render; valutare di aprire di default la sezione più rilevante (es. Consuntivo o Rate) al primo render del periodo. | S/M | NUOVO |
| SIT-003 | Low | `css/app.css` L330-332 (`.situazione-section .table-scroll, .situazione-section .data-table-wrap { max-height: ... }`) | La classe CSS `.situazione-section` non compare mai nel markup generato da `render.js` (che usa `.situazione-sections` plurale come contenitore e `.situazione-collapse-body` per il corpo di ogni accordion — verificato con grep, nessun match per `.situazione-section` in `render.js`). La regola è quindi "morta": il cap di altezza/scroll interno pensato per le tabelle di Situazione non si applica mai. | Nessun impatto visibile diretto oggi, ma è un segnale di incoerenza tra CSS e markup (token/selettori non allineati) che rende fragile ogni futura modifica di stile su questa vista. | Rinominare il selettore in `.situazione-collapse-body .data-table-wrap` (o rimuovere la regola se non più necessaria dato l'uso di `<details>`). | S | NUOVO |
| SIT-004 | Medium | `index.html` L379 (`#situazionePdfBtn`); `js/main.js` L977-985; `js/pdf-situazione.js` L12-21 (`loadPdfLibs`, import dinamico da CDN esm.sh) | Il click su "Esporta PDF" avvia un `import()` dinamico di jsPDF/autoTable da CDN esterno più la generazione del documento, ma il bottone non viene disabilitato né mostra uno stato di caricamento durante l'`await`. Su rete lenta l'utente non ha alcun feedback e può cliccare più volte, innescando generazioni/download duplicati. | Percezione di app "bloccata" o non responsiva; possibili download PDF multipli. | Disabilitare il bottone e mostrare un'etichetta tipo "Generazione…" nel `try`, ripristinare lo stato nel blocco `finally`. | S | NUOVO |
| SIT-005 | Medium | `index.html` L372-379 (nessun tooltip/`title`/hint vicino a "Preventivo", "Consuntivo", "riporti", "versamenti"); assenza di glossario in tutto il repo (verificato via grep, nessuna occorrenza di "glossario" o copy semplificata fuori dai report di analisi) | Terminologia tecnico-contabile (preventivo, consuntivo, riporto, conguaglio, "Extra da pagare" vs "Sconto/credito riportato") usata senza spiegazione contestuale in una vista densa di cifre; un condomino non esperto fatica a interpretare rapidamente la situazione economica. | Comprensione ridotta per utenti non esperti di contabilità condominiale, specialmente nei casi con saldo precedente + consuntivo combinati (`computeSituazioneTotals`, `situazione-report.js` L82-149, hint tecnici come "Versato − (consuntivo + saldo precedente)"). | Aggiungere tooltip/`title` o testo hint accanto alle etichette chiave, riusando lo stile `.hint` già presente; eventualmente copy semplificata come proposto in UX-010 ("Quanto paghi a rate" / "Spesa reale anno"). | S/M | **IRRISOLTO (rif. UX-010)** — `analysis-report.md` L60 proponeva già "Glossario tooltip + copy semplificata"; nessuna implementazione trovata nel codice attuale. |
```
